# PostgreSQL Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=postgres
POSTGRES_HOST=localhost  # Use RDS endpoint for AWS deployment
POSTGRES_PORT=5432

# Flask Configuration
FLASK_ENV=development
